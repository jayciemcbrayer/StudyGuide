// Create a program that counts to 100
// The program should count by 10

function countByTen(tens) {
    while (tens <= 100) {
        console.log(tens)
        tens = tens + 10;
    }
}

countByTen(10)